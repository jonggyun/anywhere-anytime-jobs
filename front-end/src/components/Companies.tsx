import React from 'react';
import styled from 'styled-components';

const SectionWrapper = styled.section``;

const Companies: React.FC = () => {
  return <SectionWrapper>wrapper</SectionWrapper>;
};

export default Companies;
